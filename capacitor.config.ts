import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourname.javainterviewmaster',
  appName: 'Java InterviewMaster ',
  webDir: 'dist'
};

export default config;
